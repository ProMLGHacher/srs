# Только раздача статики; наружу не публикуется — перед ним стоит voice_gateway.
server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
